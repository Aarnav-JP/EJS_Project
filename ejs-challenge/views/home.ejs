<%-include('partials/header');-%>
    <h1>Home</h1>
    <p>
        <%=content%>
        <%console.log(postarray);%>
       <% postarray.forEach(function(element){ %>
        <div>
           <h1><%=element.title%> </h1>
           <p>
            <%=element.post.substring(0,100) + ' ...' %>
            <a href="/posts/<%=element.title%>" + element.title>Read More</a>
            </p>
        </div>
       <% }); %>
    </p>


    <%-include('partials/footer');-%>